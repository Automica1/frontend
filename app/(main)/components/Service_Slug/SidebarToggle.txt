// app/components/Service_Slug/SidebarToggle.tsx
import React from 'react';
import { Menu } from 'lucide-react';

interface SidebarToggleProps {
  onToggle: () => void;
  gradient: string;
}

export default function SidebarToggle({ onToggle, gradient }: SidebarToggleProps) {
  return (
    <div className="fixed top-20 right-4 z-30 lg:hidden">
      <button
        onClick={onToggle}
        className="group relative p-3 rounded-xl font-medium transition-all duration-300 hover:scale-105"
      >
        {/* Button border with gradient */}
        <div className={`absolute inset-0 bg-gradient-to-r ${gradient} opacity-100 rounded-xl`} />
        
        {/* Button background */}
        <div className="absolute bg-black/40 backdrop-blur-xl rounded-xl" style={{
          top: '1px',
          left: '1px',
          right: '1px',
          bottom: '1px'
        }} />
        
        {/* Button content */}
        <div className="relative">
          <Menu className="w-5 h-5 text-white" />
        </div>
        
        {/* Hover glow effect */}
        <div className={`absolute inset-0 bg-gradient-to-r ${gradient} opacity-0 group-hover:opacity-20 rounded-xl blur-sm -z-10 transition-opacity duration-300`}></div>
      </button>
    </div>
  );
}